# pi_ld_global.py

# events_l        # Stores nubmer of lightning strikes in the past (10m, 30m, 1h, 24h, 30d)
# events_d        # Stores nubmer of disturber events in the past (10m, 30m, 1h, 24h, 30d)
# events_n        # Stores nubmer of noise events in the past (10m, 30m, 1h, 24h, 30d)
# events_a        # Stores nubmer of algo update events in the past (10m, 30m, 1h, 24h, 30d)
# events_last_distance # Sotres distance of last strike
# events_last_energy   # Sotres energy of last strike
# events_last_time     # Sotres datetime of last strike

# events_l = [0] * 5 # Stores nubmer of lightning strikes in the past (10m, 30m, 1h, 24h, 30d)
# events_d = [0] * 5 # Stores nubmer of disturber events in the past (10m, 30m, 1h, 24h, 30d)
# events_n = [0] * 5 # Stores nubmer of noise events in the past (10m, 30m, 1h, 24h, 30d)
# events_a = [0] * 5 # Stores nubmer of algo update events in the past (10m, 30m, 1h, 24h, 30d)
# events_last_distance = 0 # Stores distance of last strike
# events_last_energy = 0 # Stores energy of last strike
# Stores datetime of last strike